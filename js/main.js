

const gridContainer = document.getElementById('gridNav');

const cell = document.createElement('div');

cell.classList.add('cell');
// cell.setAttribute('id', 'cell-' + (i + 1) + '-' + (j + 1));
gridContainer.appendChild(cell);
// cellGrid.push(cell);


